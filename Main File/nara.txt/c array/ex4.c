#include <stdio.h>
//======>4.Ask user to input informationof 5 students. The information includesId, name and math score.<==========
// a.Display the information of allstudents.
// b.Display the information of thestudent with highest math score.
// c.Display the information of the student with lowest math score.
// d.Display the average score of math.
// e.Ask user to input an ID. Display information of student that have this ID.
int main(){
    int numOfStudent[5];
    int i, name[5], ID[5], mathScores[5];
    for (i=0; i<5; i++){
        printf("Plz input your information :\n name: \n ID: \n mathScore:");
        scanf("%d",&name[1]);
        printf("Input ID:");
        scanf("%d",&ID[2]);
    }
}